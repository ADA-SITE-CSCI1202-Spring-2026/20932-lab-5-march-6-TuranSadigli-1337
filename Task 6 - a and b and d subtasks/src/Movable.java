public interface Movable {
    // Move up (+ in y coordinate)
    void moveUp();

    // Move down (- in y coordinate)
    void moveDown();

    // Move left (- in x coordinate)
    void moveLeft();

    // Move right (+ in x coordinate)
    void moveRight();
}